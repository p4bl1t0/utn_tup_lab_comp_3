const CheckboxGates = ({ checkBox }) => {
  return (
    <div>
      <label>
        <input type="checkbox" onChange={checkBox} /> Entrada 2
      </label>
    </div>
  );
};

export default CheckboxGates;
